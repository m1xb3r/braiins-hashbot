# Copy this file to .env
# .env is git-ignored — never commit it.

# Optional: override the dashboard port (default 8000)
HASHBOT_PORT=8000

# The Braiins API key is NOT set here.
# Set it via the dashboard UI after first start:
# → open http://localhost:8000 → follow the setup prompt
